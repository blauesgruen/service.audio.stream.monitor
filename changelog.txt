v1.0.10 (2026-03-26)
- Neu: TuneIn API Integration (Describe.ashx/Tune.ashx) als zusaetzliche Now-Playing-Quelle.
- Neu: Zentrale API-Quellensteuerung (Whitelist) fuer `plugin.audio.radiode`, `plugin.audio.radio_de_light`, `plugin.audio.tunein2017`.
- Anpassung: Song-Timeout neu kalibriert auf `MB-Songlaenge - 15s`; `240s` greifen nur als Fallback, wenn keine MB-Songlaenge vorliegt.
- Neu: Debug-Properties fuer Song-Timing: `RadioMonitor.MBDurationMs`, `RadioMonitor.MBDurationS`, `RadioMonitor.TimeoutTotal`, `RadioMonitor.TimeoutRemaining`.
- Refactoring: Song-Timer-Handling zentralisiert (`_start_song_timeout`, `_update_timeout_remaining_property`, `_reset_song_timeout_state`).
- Doku: `TECHNISCHE_DOKUMENTATION.md` auf Basis des letzten Stands ueberarbeitet und auf aktuelle Logik gebracht (Whitelist, Timeout, Timer-Debug-Properties).

v1.0.9 (2026-03-11)
- Refactoring & Modularisierung: Metadaten-Logik (Parsing, Normalisierung, ICY-Extraktion) in neues Modul metadata.py ausgelagert.
- Struktur: service.py und musicbrainz.py verschlankt und für bessere Wartbarkeit optimiert.
- Verbesserung: Zentrale Verwaltung von Artist-Varianten und Titel-Bereinigung.

v1.0.8 (2026-03-09)
- Verbesserung: Numerische StreamTitle-Formate (z. B. "280220 - 391495") werden als Nicht-ICY behandelt, sodass gültige API-NowPlaying-Ergebnisse angenommen werden, anstatt numerische ICY-Strings zu verwenden.
- Dokumentation: CLAUDE.md ergänzt mit Hinweisen zur Behandlung numerischer ICY-Strings und zur Invalidierung des MusicBrainz-Caches bei Änderungen des StreamTitle.

v1.0.7 (2026-03-08)
- Refactoring: Codebasis in eigenständige Module aufgeteilt – constants.py, logger.py, cache.py, api_client.py, musicbrainz.py, radiode.py.
- Refactoring: Alle MusicBrainz-Funktionen (Query-Recording, Title-Only-Fallback, Work-Kontext, Artist-Info, Artist-Varianten, Album-Extraktion, Similarity) in musicbrainz.py ausgelagert.
- Refactoring: radio.de-Titel-Parser in radiode.py ausgelagert.
- Refactoring: Thread-safe MusicBrainz-Cache mit TTL (MusicBrainzCache) in cache.py.
- Refactoring: HTTP-Client mit Retry und Exponential-Backoff (APIClient) in api_client.py.
- Refactoring: Logging-Wrapper (log_debug/info/warning/error) in logger.py – einheitliches Präfix ohne Duplikation.
- Refactoring: Konstanten, API-URLs und Property-Namen (_P) in constants.py zentralisiert.
- service.py von 2570 auf 1483 Zeilen reduziert (kein funktionaler Unterschied).
- Fix: Vermeidung veralteter Artist/Title-Properties bei Titelwechsel: MusicBrainz-Cache wird bei ICY-StreamTitle-Wechsel invalidiert.
- Fix: radio.de-API-Ergebnisse werden nur akzeptiert, wenn sie mit dem aktuellen ICY-StreamTitle übereinstimmen (ähnlichkeitsbasiert), verzögerte API-Antworten werden verworfen.
- Fix: Song-Timeout-Handling verbessert: MB-Länge wird für Song-Timeout verwendet, damit veraltete Properties nach Ende des Songs zuverlässig gelöscht werden.

v1.0.6 (2026-03-06)
- Fix: NameError 'name _P is not defined' beim Addon-Start behoben – _P-Klasse referenzierte sich selbst während der Definition (Refactoring-Fehler aus v1.0.5).
- Fix: RadioMonitor.Station wurde bei Senderwechsel nicht aktualisiert – Station-Property wird nun bei jedem neuen Stream korrekt neu gesetzt.
- Fix: station_name wird im API-Worker-Loop bei jedem Durchlauf aus der Window-Property aktualisiert (frischer Sendername wenn Details-API nachträglich gesetzt hat).
- Fix: Station-Property wird geleert wenn kein ICY-Name vorhanden ist (kein veralteter Sendername nach Senderwechsel).
- Refactoring: Window-Property-Namen in _P-Klasse als benannte Konstanten zusammengefasst (verhindert Tippfehler, vereinfacht Umbenennung).
- Refactoring: _frd_year() als Modulfunktion ausgelagert (vorher inline an mehreren Stellen).
- Refactoring: _NUMERIC_ID_RE als Modul-Konstante (vorher mehrfach kompiliert).

v1.0.5 (2026-03-05)
- MusicPlayer-Fallback: Neue Polling-Methode für Streams ohne ICY-Metaint und ohne radio.de API (z.B. AzuraCast-Streams wie Mother Earth Radio, Ampache-Library-Streams). Erkennt Titelwechsel bei Live-Streams und verarbeitet Artist/Title via MusicBrainz. RadioMonitor.Playing wird geleert wenn kein Artist ermittelbar (z.B. Podcast ohne Interpret).
- Station ausschließlich aus ICY icy-name: MusicPlayer.Album wird nicht mehr als Stationsquelle verwendet.
- Station-ID direkt aus Logo-URL: Bei radio.de-Streams wird die Station-ID aus der Logo-URL extrahiert (z.B. radio-assets.com/300/energyclubbin.jpeg → energyclubbin). Die radio.de-Suche wird übersprungen – kein Fehlmatching mehr bei abweichenden ICY-Namen (z.B. NRJ CLUBBIN → ENERGY Clubbin').
- Stationsname via radio.de Details-API: Echter Sendername wird per Details-API geholt und in RadioMonitor.Station gesetzt, wenn Station-ID aus Logo bekannt.
- Logo-Update bei Titelwechsel: Im MusicPlayer-Fallback wird Player.Icon bei jedem Titelwechsel neu gelesen. AzuraCast-Streams liefern pro Song ein anderes Album-Cover – RadioMonitor.Logo wechselt damit mit.

v1.0.4 (2026-03-05)
- RadioMonitor.Genre wird jetzt via MusicBrainz befüllt: _musicbrainz_query_artist_info ruft den Artist-Endpoint mit inc=artist-rels+genres ab und liefert das meistgewählte Genre (nach Community-Vote-Count) zurück. Funktioniert für alle Artist-Typen (nicht nur Bands).
- Genre-Löschung vervollständigt: RadioMonitor.Genre wird nun auch gelöscht wenn kein verwertbarer StreamTitle vorliegt (artist=None und title=None), analog zu den anderen Properties.

v1.0.3 (2026-03-04)
- Album-Ermittlung präzisiert: Album wird nur noch aus Releases des tatsächlich gewählten Best-Recordings bestimmt (kein Mischen über alle Recordings).
- FirstRelease-Ermittlung präzisiert: FirstRelease wird vom Best-Recording übernommen, nicht mehr als globales Minimum über alle Treffer.
- Album-Filter verschärft: Nur echte Album-Releases ohne Compilation werden akzeptiert; wenn kein passendes Album gefunden wird, bleibt RadioMonitor.Album leer.
- Tie-Breaker für gleiches Jahr ergänzt: Bei mehreren passenden Album-Releases werden Sonder-/Store-/Deluxe-Editionen gegenüber dem Basisalbum nachrangig behandelt.

v1.0.2 (2026-03-04)
- Neue Properties RadioMonitor.BandFormed und RadioMonitor.BandMembers: Gründungsjahr und aktuelle Mitglieder einer Band, abgerufen via MusicBrainz Artist-Endpoint (inc=artist-rels). Nur für Künstler vom Typ "Group" – Solo-Künstler liefern leere Properties. In-Memory-Cache verhindert wiederholte API-Aufrufe für denselben Künstler innerhalb einer Session.
- Album-Auswahl auf Anker-Logik umgestellt: Statt des ältesten Releases wird nun das Album gewählt, dessen Erscheinungsjahr dem first_release_date des Songs am nächsten liegt. 5-stufige Qualitätspyramide (Album → Album+Compilation → unknown-type → unknown+Compilation → VA-Fallback).
- MusicBrainz Query-Limit von 100 auf 10 reduziert (schnellere Antwortzeiten, für Webradio-Content ausreichend).
- RadioMonitor.AlbumDate wird jetzt auch in _clear_properties() geleert (war bisher vergessen).
- player.clearProperty()-Aufrufe für MusicPlayer-Properties entfernt (nicht zuverlässig von Kodi unterstützt).
- Fehler-Logging verbessert: stille except-pass-Blöcke durch aussagekräftige xbmc.log-Einträge ersetzt.

v1.0.1 (2026-03-03)
- Album-Filter: Karaoke-Releases werden nun ausgeschlossen – sowohl per MusicBrainz secondary-type als auch per Titel-Heuristik ("karaoke" im Titel). Damit wird z.B. "Wellerman (Sea Shanty / Karaoke Version)" zugunsten des Original-Releases übersprungen.

v1.0.0
- Namensänderung

Archiv:
v1.1.15 (2026-03-03)
- Neue Property RadioMonitor.FirstRelease: Zeigt das Jahr der Erstveröffentlichung des Songs gemäß MusicBrainz (first-release-date). Der älteste Wert über alle zurückgegebenen Recordings wird genutzt – konsistent mit der Album-Aggregation. Die Property wird in allen Pfaden (ICY-Worker, API-Fallback-Worker) gesetzt und in clear_properties() geleert.

v1.1.14 (2026-03-02)
- Artist/Title-Reihenfolge-Erkennung grundlegend überarbeitet: Die fehlerhafte Kreuz-Validierung zwischen API- und ICY-Daten wurde entfernt. API und ICY liefern beide nur zwei Parts ohne bekannte Reihenfolge – MusicBrainz entscheidet bidirektional welcher Part Artist und welcher Title ist.
- Aktualitäts-Check für API-Daten eingeführt: Stimmt kein API-Part (Schwelle 0.6) mit einem ICY-Part überein, wird die API als veraltet (noch alter Song) verworfen und ICY als Datenquelle genutzt.
- Album-Filterlogik überarbeitet: 4-stufige Pool-Selektion (bevorzugt: kein Various-Artists, kein Live, keine Single/EP, keine Compilation). Live-Alben werden zusätzlich anhand eines Datumspräfix im Titel erkannt (z.B. "1978-12-02: Nakano Sun Plaza"). MB-Platzhaltertitel ("Unclustered Files", "[standalone recordings]") werden ausgeschlossen.
- Album-Datum auf Jahreszahl gekürzt ([:4]).
- Klammern-Bereinigung vor MB-Suche verbessert: Nur noch Klammern mit bekannten Metadaten-Keywords (Radio Edit, Remastered, Bonus usw.) werden entfernt, iterativ für gestapelte Klammern. Inhaltliche Klammern wie "(Love theme)" bleiben erhalten.
- Erweiterte Artist-Varianten für MusicBrainz-Suche: CamelCase-Splitting ("DeBurgh" → "De Burgh"), kombinierte Komma-Umkehr + CamelCase-Split, tokenbasierter no-quotes-Fallback als letztes Mittel.

v1.1.13 (2026-03-02)
- Stationsname-Filterung verbessert: der gesamte StreamTitle wird jetzt als erstes gegen den Stationsnamen verglichen (vor jedem Split und vor dem 'von'-Format-Check). Reine ICY-Senderkennung wie "NDR 90,3" wird damit zuverlässig verworfen und landet nicht mehr als Artist oder Title in den RadioMonitor-Properties.
- README.md aktualisiert: AlbumDate-Property ergänzt, Features-Liste vervollständigt, Bekannte Limitierungen präzisiert, Log-Pfade für portable und normale Installation ergänzt.

v1.1.12 (2026-03-01)
- Album-Ermittlung grundlegend überarbeitet: Releases aller Top-Recordings werden vor der Album-Auswahl zu einem gemeinsamen Pool zusammengeführt. Bisher wurde das Album pro Recording einzeln ermittelt, wodurch ein Live-Recording (zufällig erster Treffer) gegenüber einem Studio-Recording mit mehr Releases gewinnen konnte.
- Album-Filterlogik vereinfacht: Das älteste nicht-Various-Artists-Release gewinnt. Compilations erscheinen chronologisch immer nach dem Original – eine mehrstufige Keyword-/Typfilterung ist damit nicht notwendig.
- MB-Suchlimit von 25 auf 100 erhöht (MB-Maximum): Mehr Recordings im Pool bedeuten mehr Releases und damit eine zuverlässigere Album-Auswahl.
- Bugfix: Tuple-Größenfehler in _musicbrainz_query_recording und _musicbrainz_query_title_only behoben (5 statt 6 Rückgabewerte führten zu ValueError).
- Bugfix: get_nowplaying_from_apis gab bei validem Ergebnis None zurück (bare return statt return parsed_artist, parsed_title).

v1.1.11
- readme.md und Beschreibung in der addon.xml aktualisiert

v1.1.10 (2026-02-26)
- MusicBrainz Artist-Fallback erweitert: Apostroph-Varianten und Komma-Namen (Nachname, Vorname -> Vorname Nachname) werden bei 0 Treffern als Varianten getestet.
- Logging erweitert: pro Variante wird geloggt, welche Artist-Form getestet wurde und welche Variante Treffer liefert.
- Unsicheren MusicBrainz-Fallback entschärft: keine erzwungene Artist/Title-Umkehr mehr bei unklarem Ergebnis.

v1.1.9 (2026-02-26)
- MusicBrainz-MBID wird bei der Ermittlung von Artist/Title mitgeführt und im Service weiterverarbeitet.
- Neue Property `RadioMonitor.MBID` wird bei Songwechsel gesetzt bzw. bei unklaren/ungültigen Treffern aktiv geleert.
- Übergabe der MBID an das Player-Metadaten-Update erweitert (Best-Effort je nach Kodi API-Version).
- Logging um MBID-Werte in den relevanten Metadaten-Pfaden ergänzt.

v1.1.8 (2026-02-25)
- Suche bei Musicbrainz verfeinert
- Code bereinigt

v1.1.7 (2026-02-24)
- MusicBrainz-Suche grundlegend überarbeitet: Statt kombinierter Freitextsuche werden jetzt zwei gezielte Queries mit expliziten Feldangaben (recording: / artistname:) ausgeführt – behebt Fehlgriffe durch Lucene-Stopwords (z.B. "the" in "Heal the World").
- Beide Queries werden jetzt immer ausgeführt. Bisher wurde Query 2 übersprungen, wenn Query 1 einen hohen Score lieferte – auch wenn der gefundene Artist komplett falsch war (z.B. "SITER SKAIN" statt "Earth, Wind & Fire").
- Entscheidungslogik auf combined-Score (MB-Score × Artist-Ähnlichkeit) umgestellt: Ein Fehlgriff mit Score 100 aber falschen Artist wird damit zuverlässig erkannt und verworfen.
- Trefferauswertung von limit=1 auf limit=5 erweitert: Aus allen Kandidaten wird der beste anhand von Score und Artist-Ähnlichkeit gewählt.
- Nicht mehr benötigte Funktionen _musicbrainz_artist_score(), _mb_result_plausible() und _musicbrainz_find_recording() entfernt; toter Code unterhalb eines frühen return beseitigt.

v1.1.6 (2026-02-24)
- parsing bei Musicbrainz verfeinert

v1.1.5 (2026-02-23)
- Priorisierung der Metadaten-Quellen verbessert: API-Daten werden bevorzugt, MusicBrainz dient nur noch zur Validierung und überschreibt keine korrekten API-Ergebnisse mehr.
- Fehlerhafte Artist/Title-Zuordnung bei ICY-Metadaten behoben (z.B. "Pretenders - Don't get me wrong" wird jetzt korrekt erkannt).
- Logging erweitert: paralleles Logging von ICY- und API-Daten, detaillierte Ausgaben zu allen Metadaten-Quellen und Entscheidungen.
- Robustheit erhöht: Fehlerbehandlung und Fallback-Logik für ungültige oder fehlende Metadaten optimiert.
- Kommentare und Docstrings aktualisiert und präzisiert.

v1.1.4 (2026-02-23)
- Sofortiges Erfassen und Setzen des Logos beim Streamstart verbessert (onAVStarted prüft jetzt auch Video/Dateien, Logging erweitert)
- Refactoring: Artist/Title-Erkennung in MusicBrainz-Queries optimiert, überflüssige Tie-Breaker entfernt, Kommentare präzisiert
- StreamTitle-Parsing überarbeitet: MusicBrainz wird immer zur Validierung genutzt, Logging von ICY- und API-Daten verbessert
- Verbesserte Artist/Title-Matching-Logik in MusicBrainz (exakte Matches, Score-basierte Auswahl, detailliertes Logging)
- Retry-Logik und Logging in MusicBrainz-Score-Funktionen erweitert, Docstrings und Kommentare aktualisiert

v1.1.3 (2026-02-22)
- MusicBrainz-Recording-Abfrage ersetzt Artist-Score-Logik für bessere Zuordnung
- Score-Retry bei Verbindungsfehlern (max. 2 Wiederholungen)
- Logging aller Recording-Ergebnisse (Score, Artist, Title, MBID)
- Kommentare und Docstrings aktualisiert

v1.1.2 (2026-02-21)
- Umbenannt zu "Audio Stream Monitor" (universeller Name)
- Beschreibungen aktualisiert: Addon ist nicht auf Radio.de beschränkt
- Unterstützt alle Addons, die HTTP/HTTPS Audio-Streams abspielen

v1.1.1 (2026-02-19)
- Korrigierte Titel/Artist Reihenfolge für radio.de API (Title - Artist)
- Player InfoTag Update für Standard Kodi InfoLabels (MusicPlayer.Artist, MusicPlayer.Title)
- Artist Slideshow Kompatibilität (artistslideshow.artistname/trackname)
- Sender-spezifische Format-Erkennung (NDR, WDR, SWR, etc.)
- Logo-Support aus verschiedenen Quellen (Player.Art(poster), Player.Icon, etc.)
- Verbesserte Metadaten-Priorisierung (API-first für präzise Daten)

v1.0.1 (Initial)
- ICY-Metadaten Parsing
- radio.de API Integration
- Window Properties für Skins
